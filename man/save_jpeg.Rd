% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/S04_Plotting.R
\name{save_jpeg}
\alias{save_jpeg}
\alias{save_jpg}
\title{Save JPEG file}
\usage{
save_jpeg(
  file_name,
  fun_plot,
  dmn = c(5, 5),
  units = "in",
  res = 300,
  quality = 90,
  pointsize = 12,
  bg = "white",
  family = "",
  type = c("windows", "cairo"),
  symbolfamily = "default",
  return_file_name = FALSE,
  ...
)

save_jpg(
  file_name,
  fun_plot,
  dmn = c(5, 5),
  units = "in",
  res = 300,
  quality = 90,
  pointsize = 12,
  bg = "white",
  family = "",
  type = c("windows", "cairo"),
  symbolfamily = "default",
  return_file_name = FALSE,
  ...
)
}
\arguments{
\item{file_name}{A character string, the
desired path to the PNG file.}

\item{fun_plot}{A function that generates
a figure as output.}

\item{dmn}{A numeric vector of two values,
the width and height of the figure
to save (default is in inches).}

\item{units}{A character string, the
units for figure dimensions (see
\link[grDevices:png]{grDevices::jpeg}).}

\item{res}{An integer value, the
figure resolution (see
\link[grDevices:png]{grDevices::jpeg}).}

\item{quality}{A numeric value from 0 100, a
percentage governing compression (lower values
give more compression but worse quality; see
\link[grDevices:png]{grDevices::jpeg}).}

\item{pointsize}{An integer value, the point
size of text (see \link[grDevices:png]{grDevices::jpeg}).}

\item{bg}{a character string, the background
color (see \link[grDevices:png]{grDevices::jpeg}).}

\item{family}{A character string specifying the
font family (see \link[grDevices:png]{grDevices::jpeg}).}

\item{type}{A character string governing how
plotting is done (see \link[grDevices:png]{grDevices::jpeg}).}

\item{symbolfamily}{A character string for
cairographics options (see \link[grDevices:png]{grDevices::jpeg}).}

\item{return_file_name}{A logical value,
if \code{TRUE} returns the file name
as a character string.}

\item{...}{Additional arguments for the
\code{fun_plot} function.}
}
\value{
A JEPG file, and optionally the
file name.
}
\description{
Function that generates a figure and saves it
as a JPEG file using common settings.
}
